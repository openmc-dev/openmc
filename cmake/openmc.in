#!/usr/bin/env python3

import os
import sys
import sysconfig


if __name__ == "__main__":
    os.execv(
        os.path.join(sysconfig.get_path("platlib"), 'openmc', '@CMAKE_INSTALL_BINDIR@', 'openmc'),
        sys.argv,
    )